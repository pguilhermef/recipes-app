import React from 'react';

function SearchBar() {
  return (
    <input
      className="form-control"
      type="search"
      placeholder="Search"
      aria-label="Search"
    />
  );
}

export default SearchBar;
